# Copy to deploy/local-postgres.env on Ubuntu, with a unique random password.
# This file is read only by the PostgreSQL container.
POSTGRES_DB=trade_cognition
POSTGRES_USER=trade_writer
POSTGRES_PASSWORD=REPLACE_WITH_RANDOM_SECRET
